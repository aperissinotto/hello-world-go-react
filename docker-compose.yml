version: '3.8'

services:
  backend:
    build: ./backend
    container_name: go-backend
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: react-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend